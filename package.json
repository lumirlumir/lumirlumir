{
  "private": true,
  "name": "lumirlumir",
  "type": "module",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "^24.18.0"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "^24.18.0",
      "onFail": "error"
    },
    "packageManager": {
      "name": "npm",
      "version": ">=11.16.0",
      "onFail": "error"
    }
  },
  "scripts": {
    "prepare": "node --run sync-client && husky",
    "build": "node src/sync-server.js",
    "build:s": "sponsorkit",
    "fix": "npm-run-all -c -l -p fix:*",
    "fix:eslint": "eslint --fix --cache --cache-strategy content",
    "fix:prettier": "prettier . --write --ignore-unknown",
    "fix:markdownlint": "markdownlint **/*.md --fix",
    "lint": "npm-run-all -c -l -p lint:*",
    "lint:eslint": "eslint --cache --cache-strategy content",
    "lint:prettier": "prettier . --check --ignore-unknown",
    "lint:editorconfig": "editorconfig-checker -config .editorconfig-checker.json",
    "lint:markdownlint": "markdownlint **/*.md",
    "sync-client": "node src/sync-client.js"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "editorconfig-checker": "^6.1.1",
    "eslint": "^9.39.5",
    "eslint-config-bananass": "^0.8.0",
    "eslint-markdown": "^0.12.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "markdownlint-cli": "^0.49.1",
    "npm-run-all2": "^9.0.3",
    "prettier": "^3.9.6",
    "sponsorkit": "^18.0.0"
  }
}
